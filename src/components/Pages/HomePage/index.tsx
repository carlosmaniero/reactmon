import * as React from 'react';
import { SpeciesList } from '../../Species/SpeciesList/container';

export default class HomePage extends React.Component {
    render() {
        return (
            <SpeciesList/>
        );
    }
}